package reflex.core
{
	import mx.core.UIComponent;
	
	// used as a base class only when compiling for use with Flex
	public class FlexComponent extends UIComponent implements IMeasurable, IMeasurableRange, IPercentSize
	{
		
	}
}